# Railway 服务配置
[http_service]
  internal_port = 8000
  force_https = true
  zero_downtime_deploy = true

# 环境变量
[env]
  PYTHONUNBUFFERED = "1"
  DEBUG = "false"
  PORT = "8000"

# 健康检查
[deploy]
  healthcheck_path = "/health"